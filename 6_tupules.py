# Tuple are immutable lists 
myTuple = (1,2,3,4,5,5,6,7)
# Now this tupule cannot be changed like reversing , updating, sorting etc but we can perform other actions

print(myTuple[1])
print(3 in myTuple)

#Methods

print(myTuple.count(4))
print(myTuple.index(5))